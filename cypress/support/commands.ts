/// <reference types="cypress" />

// Cypress.Commands.add('login', (token: string) => {
//     cy.visit('/auth');
//     cy.get('input#token').type(token);
//     cy.contains('Authenticate with GitHub').click();
//     cy.url().should('include', '/dashboard');
//   });
  // cypress/support/commands.ts

